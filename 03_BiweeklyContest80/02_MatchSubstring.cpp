// https://leetcode.com/contest/biweekly-contest-80/problems/match-substring-after-replacement/

// You are given two strings s and sub. You are also given a 2D character 
// array mappings where mappings[i] = [oldi, newi] indicates that you may 
// replace any number of oldi characters of sub with newi. Each character in 
// sub cannot be replaced more than once.

// Return true if it is possible to make sub a substring of s by replacing 
// zero or more characters according to mappings. Otherwise, return false.

// A substring is a contiguous non-empty sequence of characters within a string.

#include <bits/stdc++.h>
using namespace std ;

class Solution {
public:
    bool matchReplacement(string s, string sub, vector<vector<char>>& mappings) {
        
    }
};

int main () {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    return 0;
}